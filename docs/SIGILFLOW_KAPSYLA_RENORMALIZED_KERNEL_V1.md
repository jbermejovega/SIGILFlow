# SIGILFLOW_KAPSYLA_RENORMALIZED_KERNEL_V1

`SIGILFlow` is sealed by the resident SIGILBOOK KAPSYLA ordering:

```text
COAXIAL_MERGE
-> SKAFFO
-> STABILIZE
-> WEAVE
-> NORMALIZE
-> RENORMALIZE
-> PACAPDG
-> UAP
-> SAFE_REPLAY
-> KAPSYLA
```

Reference canon: `jbermejovega/sigilbook@e21451ae9bb50af2ffee1756d7bace4592365592`.

The seal is applied to the merged SIGILFlow presheaf/sheaf-preimage + join/meet + AST-renormalization kernel. KAPSYLA is a terminal admissibility/sealing layer, not an alias for the AST, normalization, source repositories, or runtime execution.

Hard laws:

```text
NORMALIZE != RENORMALIZE
SAME_PROJECTION != SAME_BEARER
KAPSYLA != TOTAL_AST
KAPSYLA != RUNTIME_SUCCESS
PACAPDG < UAP < SAFE_REPLAY < KAPSYLA
IDENTITY_TRANSPORT = FALSE
AUTHORITY_TRANSPORT = FALSE
SCHEDULER_TRANSPORT = FALSE
RUNTIME_TRANSPORT = FALSE
```

A KAPSYLA receipt requires a fresh renormalized occurrence plus explicit PACAPDG/UAP/Safe-Replay admission. Reusing the normalized projection digest as the occurrence digest is rejected.

PLURA MANENT. PIORNALEGO ES CANON.
